- create more tests for actions
- test what happens when the change is a file delete
- update docs to explain new commands and how to use them
- create a bundle command
    - Can generate a key (lets use PQC)
    - Given a folder it signs all the files with a provided key
    - Rule engine checks every rule signature on load
    - If there's an inconsistency it can report it to a specific url
        - Url can be specified via MDM policy
    - Update docs explaining how to protect bundle.
        - Explain in that it only works if a signing key is provided in the mdm policy, and its recommended to do so
- Create a script to install the tool as a service
    - Create an MDM policy to install through the policy
- Documents that inline policies cannot be tampered with, but can only be updated by new policy
- Create a new policy config "groups", array of strings. Create the groups field in the rule files as well. If a rule file has the group field, it only runs if the policy groups contain any of those groups
